import { Container } from 'components/constants/Container.styled';
import Loader from 'components/Loader';

const RefreshingUserLoader = () => {
  return (
    <Container>
      <Loader />
    </Container>
  );
};

export default RefreshingUserLoader;
